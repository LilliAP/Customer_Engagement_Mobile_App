import 'package:flutter/material.dart';

class PrivateMessageScreen extends StatelessWidget {
  final receiverId;
  const PrivateMessageScreen({super.key, this.receiverId});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
